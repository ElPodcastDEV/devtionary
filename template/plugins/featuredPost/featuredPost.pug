.wordOfTheDay
  .title Palabra del día
  - const currentDate = new Date();
  .date #{currentDate.getDate()} de #{currentDate.toLocaleString('es-MX', { month: 'long' })}, #{currentDate.getFullYear()}
  .wordName: a(href=`${siteUrl}/posts/${path}`) #{title}
  .pronuntiation #{pronuntiation}
  p #{excerpt}...
  a(href=`${siteUrl}/posts/${path}`) Leer más

style.
  .wordOfTheDay {
    background-color: #f9f9f9;
    padding: 20px;
    margin-bottom: 20px;
    border: 4px solid #006fe6;
    border-radius: 10px;
  }
  .wordOfTheDay .title {
    font-size: 1.4em;
    font-weight: 400;
    color: #006fe6;
  }
  .wordOfTheDay .wordName {
    font-size: 2em;
    font-weight: bold;
    color: #006fe6;
    margin: 10px 0;
  }
  .wordOfTheDay .wordName a {
    text-decoration: none;
    color: inherit;
  }
  .wordOfTheDay .date {
    font-size: 0.8em;
    color: #666;
    text-transform: uppercase;
    margin: 5px 0 10px 0;
  }
  .wordOfTheDay .pronuntiation {
    font-size: 1.2em;
    color: #666;
    margin: 10px 0;
  }
